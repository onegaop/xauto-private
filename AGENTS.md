# XAuto Agent Rules

1. `git` 提交前，必须移除新增的 `console.log`。
2. 避免在 `master` 分支提交（使用 `main` 或功能分支）。
3. 只要发生提交，必须同时 `push` 到远端。
4. 涉及页面设计 / UI 设计任务时，必须优先使用 `taste-skill`：
   `/Users/eagleone/.codex/skills/taste-skill/SKILL.md`
